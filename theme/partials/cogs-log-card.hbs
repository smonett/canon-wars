{{!-- Cog's Log Card — distinct visual treatment for posts tagged cogs-log --}}
<article class="post-card cogs-log-card {{post_class}}">

    {{!-- Cog's Log indicator strip --}}
    <div class="cogs-log-indicator" aria-label="Cog's Log entry">
        <span class="gear-icon" aria-hidden="true">⚙️</span>
        <span class="cogs-log-label">Cog's Log</span>
    </div>

    {{!-- Featured image --}}
    {{#if feature_image}}
    <a href="{{url}}" class="post-card-image-link" aria-hidden="true" tabindex="-1">
        <img
            class="post-card-image"
            src="{{img_url feature_image size="m"}}"
            srcset="{{img_url feature_image size="s"}} 300w,
                    {{img_url feature_image size="m"}} 600w,
                    {{img_url feature_image size="l"}} 1000w"
            sizes="(max-width: 600px) 100vw, (max-width: 1200px) 50vw, 400px"
            alt="{{title}}"
            loading="lazy"
        >
    </a>
    {{/if}}

    <div class="post-card-body">

        {{!-- Title --}}
        <h2 class="post-card-title">
            <a href="{{url}}">{{title}}</a>
        </h2>

        {{!-- Excerpt --}}
        {{#if excerpt}}
        <p class="post-card-excerpt">{{excerpt words="28"}}</p>
        {{/if}}

        {{!-- Footer meta --}}
        <footer class="post-card-footer">
            <time class="post-card-date" datetime="{{date format="YYYY-MM-DD"}}">
                {{date format="MMM D, YYYY"}}
            </time>
            <span class="post-card-sep" aria-hidden="true">&middot;</span>
            <span class="post-card-reading-time">{{reading_time}}</span>
        </footer>

    </div>
</article>
